
<div class="tytul"><h1>Tweety <%= link_to "nowy", new_tweet_path %></h1></div>
  <div class="tweets">
      <% @tweets.each do |tweet| %>
        <div class="tweet clearfix">
          <%= image_tag tweet.user.image_url %>
          <div class="tweet-content">
              <h3>
                <%= tweet.user.nazwa %>
                 <%= link_to "usuń", tweet_path(tweet),
                   :method => :delete %>
                  <%= link_to "edytuj", edit_tweet_path(tweet) %>
              </h3>
              <p>
                <%= tweet.content %>
              </p>
          </div>
        </div>
      <% end %>
  </div>